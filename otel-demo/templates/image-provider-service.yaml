apiVersion: v1
kind: Service
metadata:
  name: image-provider
  labels:
    app: image-provider
    {{- include "otel-demo.labels" . | nindent 4 }}
spec:
  selector:
    app: image-provider
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP