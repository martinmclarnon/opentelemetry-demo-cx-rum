# file: .github/workflows/docker-publish.yaml
#  This workflow builds and pushes Docker images for the OpenTelemetry demo CX RUM application
#  whenever changes are pushed to the main branch, specifically in the 'src' directory or
#  the workflow file itself.
#  It uses the GitHub Container Registry (GHCR) for storing the images.
#  The images are built with a specific OpenTelemetry Java agent version and pushed with the 'latest' tag.
#  The workflow is triggered on pushes to the main branch and monitors changes in the specified paths
name: Build and Push Images to GHCR

on:
  push:
    branches: [ main ]
    paths:
      - 'src/**'
      - '.github/workflows/docker-publish.yaml'
  workflow_dispatch:

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    env:
      REGISTRY: ghcr.io
      IMAGE_PREFIX: ghcr.io/${{ github.repository }}

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 # Ensure full history for git diff

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Fetch Git history
        run: |
          git fetch --prune --unshallow || true
          git fetch --deepen=50 || true

      - name: Build and push images
        run: |
          IMAGES="accounting ad cart checkout currency email fraud-detection frontend frontend-proxy image-provider kafka load-generator payment product-catalog quote recommendation shipping kafka flagd-ui"
          BASE_SHA=$(git rev-parse HEAD~1 || echo "")
          if [ -z "$BASE_SHA" ] || ! git cat-file -e $BASE_SHA 2>/dev/null; then
            echo "⚠️ Base commit missing or invalid, falling back to HEAD"
            BASE_SHA=$(git rev-parse HEAD)
          fi

          CHANGED=$(git diff --name-only $BASE_SHA HEAD)

          for IMAGE in $IMAGES; do
            DOCKERFILE_PATH="src/$IMAGE/Dockerfile"
            # Special case for cart service
            if [ "$IMAGE" = "cart" ]; then
              DOCKERFILE_PATH="src/cart/src/Dockerfile"
            fi

            if echo "$CHANGED" | grep -q "^src/$IMAGE/"; then
              echo "🔨 Building and pushing $IMAGE (changed) as tag latest..."
              docker build \
                -f $DOCKERFILE_PATH \
                --build-arg OTEL_JAVA_AGENT_VERSION=1.33.0 \
                --build-arg OPENTELEMETRY_CPP_VERSION=1.18.0 \
                -t $IMAGE_PREFIX-$IMAGE:latest .
              docker push $IMAGE_PREFIX-$IMAGE:latest
            else
              echo "⏭️ Skipping $IMAGE (no changes)"
            fi
          done

